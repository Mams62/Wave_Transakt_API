spring.application.name=wave-transakt-api
server.port=8080

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/wave_transakt}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:postgres}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

paystack.secret-key=${PAYSTACK_SECRET_KEY:}
paystack.base-url=https://api.paystack.co

# Required outside local development. Must be a Base64-encoded 256-bit+ secret.
wavetransakt.security.jwt-secret=${WAVETRANSAKT_JWT_SECRET:}

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never
